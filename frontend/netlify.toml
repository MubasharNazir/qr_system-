# Netlify configuration (alternative to Cloudflare Pages)
# If using Netlify instead of Cloudflare

[build]
  command = "npm install && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
